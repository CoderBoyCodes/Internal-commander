let user = prompt("What's your username?"); let console = navigator.platform; let log = ("user=" + user + " platform=" + console + " type=manualCrypt"); alert("Logged In Sucessfully! log(" + log + ")"); let cmd = prompt("What's your command? (in development...)"); if (cmd == "/platform") { let input = navigator.platform; alert("input = (" + input + ")") } else { alert("Command '" + cmd + "' doesn't work or system is in development") }
